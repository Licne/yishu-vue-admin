export interface ProductsSearchData extends Pagination {
    // page:number;
    // pageSize:number;
    // 关键词
    attribute:string;
    // 用户名
    owner:string;

}

export interface ProductsResult {
    // id 
    id:number;
    name:string;
    categoryNa
}